---
/**
 * TicketCard Component - STL Festival 2026
 * Card de ingresso reutilizável com design inspirado no Klangtherapie Festival
 *
 * @component
 * @example
 * <TicketCard
 *   name="Camarote Premium"
 *   description="Área VIP com bar exclusivo"
 *   soldOut={true}
 *   variant="premium"
 *   ariaLabel="Camarote Premium - Esgotado"
 * />
 */

export interface Props {
  name: string;
  price?: string;
  description: string;
  soldOut?: boolean;
  variant: 'premium' | 'standard';
  ariaLabel: string;
  buyNowLabel: string;
}

const {
  name,
  price,
  description,
  soldOut = false,
  variant,
  ariaLabel,
  buyNowLabel,
} = Astro.props;

// Definir cores baseadas no variant
const variantClasses = {
  premium: 'ticket-card--premium',
  standard: 'ticket-card--standard',
};
---

<article
  class={`ticket-card ${variantClasses[variant]} ${soldOut ? 'ticket-card--sold-out' : ''}`}
  aria-label={ariaLabel}
>
  <!-- Furo circular do ticket (parte superior) -->
  <div class="ticket-card__hole" aria-hidden="true"></div>

  <!-- Linha tracejada abaixo do furo -->
  <div class="ticket-card__perforation" aria-hidden="true"></div>

  <!-- Badge "Sold Out" diagonal (se aplicável) -->
  {
    soldOut && (
      <div class="ticket-card__sold-out-banner" aria-hidden="true">
        <span class="ticket-card__sold-out-text">Sold out</span>
      </div>
    )
  }

  <!-- Header do card com nome do ticket -->
  <header class="ticket-card__header">
    <h3 class="ticket-card__name">{name}</h3>
  </header>

  <!-- Círculo com preço (inspirado no Klangtherapie) -->
  <div class="ticket-card__price-circle" aria-hidden="true">
    {price && <span class="ticket-card__price">{price}</span>}
    {!price && soldOut && <span class="ticket-card__price">-</span>}
  </div>

  <!-- Descrição do ingresso -->
  <div class="ticket-card__description">
    <p>{description}</p>
  </div>

  <!-- Botão de compra (ou disabled se sold out) -->
  <div class="ticket-card__action">
    {
      soldOut ? (
        <button
          type="button"
          class="ticket-card__button ticket-card__button--disabled"
          disabled
          aria-disabled="true"
        >
          Esgotado
        </button>
      ) : (
        <a
          href="#"
          class="ticket-card__button"
          aria-label={`${buyNowLabel} - ${name}`}
        >
          {buyNowLabel}
        </a>
      )
    }
  </div>
</article>

<style>
  /* ========== CARD BASE ========== */
  .ticket-card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    min-height: 520px;
    padding: calc(var(--spacing-2xl) + var(--spacing-lg)) var(--spacing-2xl)
      var(--spacing-2xl);
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-lg);
    transition:
      transform var(--duration-normal) var(--ease-out),
      box-shadow var(--duration-normal) var(--ease-out);
    overflow: visible; /* Permite o furo circular ficar visível */
  }

  .ticket-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  /* ========== TICKET HOLE (Furo circular no topo) ========== */
  .ticket-card__hole {
    position: absolute;
    top: 24px;
    left: 50%;
    transform: translateX(-50%);
    width: 32px;
    height: 32px;
    background-color: var(--color-cream);
    border-radius: var(--radius-full);
    box-shadow:
      inset 0 2px 4px rgba(0, 0, 0, 0.2),
      0 1px 2px rgba(0, 0, 0, 0.1);
    z-index: 5;
  }

  /* ========== TICKET PERFORATION (Linha tracejada) ========== */
  .ticket-card__perforation {
    position: absolute;
    top: 68px;
    left: 0;
    width: 100%;
    height: 2px;
    background-image: repeating-linear-gradient(
      to right,
      currentColor 0,
      currentColor 8px,
      transparent 8px,
      transparent 16px
    );
    opacity: 0.3;
    z-index: 5;
  }

  /* ========== VARIANTS ========== */
  /* Premium (Sold Out) - Grayscale */
  .ticket-card--premium {
    background: linear-gradient(
      135deg,
      var(--color-gray-300) 0%,
      var(--color-gray-400) 100%
    );
    color: var(--color-gray-700);
  }

  /* Standard - Gradiente verde para camarote, azul para pista */
  .ticket-card--standard:nth-of-type(2) {
    background: linear-gradient(
      135deg,
      var(--color-green) 0%,
      var(--color-green-light) 100%
    );
    color: var(--color-cream);
  }

  .ticket-card--standard:nth-of-type(3) {
    background: linear-gradient(
      135deg,
      var(--color-blue) 0%,
      var(--color-blue-light) 100%
    );
    color: var(--color-cream);
  }

  /* ========== SOLD OUT BANNER (Faixa diagonal) ========== */
  .ticket-card__sold-out-banner {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 140%;
    padding: var(--spacing-lg) 0;
    background-color: var(--color-black);
    color: var(--color-white);
    transform: translate(-50%, -50%) rotate(-35deg);
    text-align: center;
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 2.5rem;
    letter-spacing: 0.1em;
    text-transform: lowercase;
    box-shadow:
      0 4px 8px rgba(0, 0, 0, 0.3),
      0 -4px 8px rgba(0, 0, 0, 0.3);
    z-index: 15;
  }

  /* Garantir que a faixa fique contida no card */
  .ticket-card--sold-out {
    overflow: hidden;
  }

  .ticket-card__sold-out-text {
    display: block;
    font-style: italic;
  }

  /* ========== HEADER ========== */
  .ticket-card__header {
    text-align: center;
    margin-top: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
  }

  .ticket-card__name {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    font-weight: 700;
    line-height: 1.2;
    margin: 0;
  }

  /* ========== PRICE CIRCLE ========== */
  .ticket-card__price-circle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 180px;
    height: 180px;
    border-radius: var(--radius-full);
    background-color: rgba(255, 255, 255, 0.15);
    border: 4px solid rgba(255, 255, 255, 0.3);
    margin: var(--spacing-xl) 0;
    backdrop-filter: blur(10px);
  }

  .ticket-card__price {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    font-weight: 700;
    line-height: 1;
  }

  /* ========== DESCRIPTION ========== */
  .ticket-card__description {
    text-align: center;
    margin-bottom: var(--spacing-xl);
    padding: 0 var(--spacing-md);
  }

  .ticket-card__description p {
    font-family: var(--font-body);
    font-size: 1rem;
    line-height: 1.6;
    margin: 0;
    opacity: 0.95;
  }

  /* ========== ACTION BUTTON ========== */
  .ticket-card__action {
    width: 100%;
    margin-top: auto;
  }

  .ticket-card__button {
    display: block;
    width: 100%;
    padding: var(--spacing-md) var(--spacing-xl);
    border: none;
    border-radius: var(--radius-lg);
    font-family: var(--font-heading);
    font-size: 1.125rem;
    font-weight: 600;
    text-align: center;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    cursor: pointer;
    transition:
      background-color var(--duration-fast) var(--ease-out),
      transform var(--duration-fast) var(--ease-out);
  }

  /* Button para cards standard (verde/azul) */
  .ticket-card--standard .ticket-card__button {
    background-color: var(--color-cream);
    color: var(--color-green-dark);
  }

  .ticket-card--standard .ticket-card__button:hover {
    background-color: var(--color-cream-light);
    transform: scale(1.02);
  }

  .ticket-card--standard .ticket-card__button:focus-visible {
    outline: 3px solid var(--color-cream);
    outline-offset: 2px;
  }

  /* Button disabled (sold out) */
  .ticket-card__button--disabled {
    background-color: var(--color-gray-600);
    color: var(--color-gray-300);
    cursor: not-allowed;
    opacity: 0.7;
  }

  .ticket-card__button--disabled:hover {
    transform: none;
  }

  /* ========== SOLD OUT STATE ========== */
  .ticket-card--sold-out {
    opacity: 0.85;
  }

  .ticket-card--sold-out:hover {
    transform: none;
  }

  /* ========== RESPONSIVE ========== */
  @media (max-width: 768px) {
    .ticket-card {
      min-height: 480px;
      padding: calc(var(--spacing-xl) + var(--spacing-md)) var(--spacing-xl)
        var(--spacing-xl);
    }

    .ticket-card__hole {
      width: 28px;
      height: 28px;
      top: 20px;
    }

    .ticket-card__perforation {
      top: 60px;
    }

    .ticket-card__sold-out-banner {
      font-size: 2rem;
      padding: var(--spacing-md) 0;
    }

    .ticket-card__name {
      font-size: 1.5rem;
    }

    .ticket-card__price-circle {
      width: 150px;
      height: 150px;
    }

    .ticket-card__price {
      font-size: 2rem;
    }

    .ticket-card__description p {
      font-size: 0.9375rem;
    }

    .ticket-card__button {
      font-size: 1rem;
      padding: var(--spacing-md);
    }
  }

  @media (max-width: 480px) {
    .ticket-card {
      min-height: 440px;
    }

    .ticket-card__hole {
      width: 24px;
      height: 24px;
      top: 18px;
    }

    .ticket-card__perforation {
      top: 54px;
    }

    .ticket-card__sold-out-banner {
      font-size: 1.5rem;
      padding: var(--spacing-sm) 0;
    }

    .ticket-card__price-circle {
      width: 130px;
      height: 130px;
    }

    .ticket-card__price {
      font-size: 1.75rem;
    }
  }

  /* ========== ACESSIBILIDADE ========== */
  @media (prefers-reduced-motion: reduce) {
    .ticket-card,
    .ticket-card__button {
      transition: none;
    }

    .ticket-card:hover {
      transform: none;
    }
  }
</style>
